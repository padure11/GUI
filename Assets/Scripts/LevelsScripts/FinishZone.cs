using UnityEngine;

// Marker component. A robot counts as "on finish" when a downward raycast
// from it (PlayerController.IsOnFinish) hits an object carrying this component.
public class FinishZone : MonoBehaviour
{
}
